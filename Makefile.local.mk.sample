## Adapt this file as a local Makefile.local.mk (never commit to the repository)
## configuration for testing
# Just execute the same targets on test data


IMPRESSO_REBUILT_DATA_DIR ?= test/rebuilt-data

BUILD_DIR ?= testbuild
